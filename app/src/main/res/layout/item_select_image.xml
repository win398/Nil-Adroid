<?xml version="1.0" encoding="UTF-8" ?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="2dp"
    android:id="@+id/item_select_container"
    android:background="@color/bg_default"
    android:orientation="vertical">
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/item_select_image_container">
        <ImageView
            android:id="@+id/item_select_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@mipmap/header"
            android:contentDescription="@string/todo" />
        <ImageView
            android:id="@+id/item_select_delete"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@mipmap/delete_image"
            android:scaleType="fitXY"
            android:layout_alignParentEnd="true"
            android:layout_alignParentTop="true"
            android:contentDescription="@string/todo" />
    </RelativeLayout>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_image_bound"
        android:gravity="center"
        android:padding="20dp"
        android:id="@+id/item_select_trigger"
        android:visibility="gone">
        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@mipmap/add_image"
            android:contentDescription="@string/todo" />
    </LinearLayout>
</LinearLayout>